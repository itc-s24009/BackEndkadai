extends ../layout

prepend head
    - const title = 'Register'

block header
    h1(class=['text-5xl/14', 'font-light', 'mb-4']) ユーザー登録

block content
    form(method='post' action='/users/register' class=['max-w-sm', 'mx-auto'])

        // ▼ 名前入力欄を追加しました
        .form-group
            input(type='text' name='name' id='name' placeholder='' required
            autocomplete='name').peer
            label(for='name') お名前

        // ▼ メールアドレス
        .form-group
            input(type='email' name='email' id='email' placeholder='' required
            autocomplete='username').peer
            label(for='email') メールアドレス

        // ▼ パスワード
        .form-group
            input(type='password' name='password' id='password' required
            placeholder='' autocomplete='new-password').peer
            label(for='password') パスワード

        button(type='submit').submit-button 登録する

    // ▼ ログイン画面へのリンクに変更
    p(class=['mt-4', 'flex', 'place-content-center', 'text-sm'])
        span すでに登録済みの方は
        a(href='/users/login' class=['ml-1', 'hover:underline', 'hover:text-blue-600'])
            | ログインへ

    if error
        p(class=['mt-6', 'text-sm', 'text-red-600', 'dark:text-red-400',
            'text-center'])= error